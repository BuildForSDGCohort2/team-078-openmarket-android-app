<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.SplashScreen">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/launch_screen</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar">
        <item name="android:itemTextAppearance">@style/TextAppearance</item>
        <item name="android:actionMenuTextColor">@color/colorTextPrimary</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="colorControlNormal">@color/colorTextPrimary</item>
    </style>

    <style name="TextAppearance" parent="@style/AppTheme.NoActionBar">
        <item name="actionMenuTextColor">@color/colorTextPrimary</item>
        <item name="android:actionMenuTextColor">@color/colorTextPrimary</item>
    </style>

    <style name="BottomNavigationTheme" parent="AppTheme">
        <item name="itemTextAppearanceActive">@style/BottomNavView.ActiveState</item>
        <item name="itemTextAppearanceInactive">@style/BottomNavView.InactiveState</item>
    </style>

    <style name="BottomNavView.ActiveState" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">10sp</item>
        <item name="fontFamily">@font/poppins_bold</item>
    </style>

    <style name="BottomNavView.InactiveState" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">10sp</item>
        <item name="fontFamily">@font/poppins_regular</item>
    </style>

</resources>